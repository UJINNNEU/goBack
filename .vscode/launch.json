{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch Package",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "D:/ApplicationBackend/cmd/main.go",  // ← явный путь
            "cwd": "${workspaceFolder}"
        }
    ]
}